---
# GENERATED - Ubuntu noble / amd64. Regenerate with scripts/apt-pins-refresh.py;
# Renovate keeps the values current through the `deb` custom managers in
# .github/renovate.json, so edit this by hand only to add or remove a name.

github_cli_prereqs_apt_pins:
  wget: "1.21.4-1ubuntu4.5"

github_cli_apt_pins:
  gh: "2.101.0"
