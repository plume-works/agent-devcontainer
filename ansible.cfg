[defaults]
# Lives at the repository root so every Ansible command runs from there and picks
# this up automatically. Relative paths below resolve against this file's
# directory, not the current working directory.
inventory=ansible/inventories/localhost.yml
log_path=ansible/ansible.log
playbook_vars_root=top
roles_path=ansible/roles
