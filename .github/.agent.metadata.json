{
  "iwe-map": {
    "digest_ignore": {
      "**/*.yml": [
        {
          "pattern": "(uses: [^@\\s]+)@v[0-9]+(\\.[0-9]+)*",
          "replace": "\\1@v<PIN>",
          "reason": "Renovate automerges github-actions version bumps (.github/renovate.json)."
        },
        {
          "pattern": "version: '[0-9]+\\.[0-9]+\\.[0-9]+'",
          "replace": "version: '<PIN>'",
          "reason": "Tool versions pinned to actions, bumped by the same automerge."
        },
        {
          "pattern": "ubuntu-[0-9]+\\.[0-9]+",
          "replace": "ubuntu-<PIN>",
          "reason": "Renovate bumps the GitHub-hosted runner labels (github-runner datasource, .github/renovate.json); the -arm suffix stays unmasked so an architecture change still counts."
        },
        {
          "pattern": "@sha256:[0-9a-f]{64}",
          "replace": "@sha256:<DIGEST>",
          "reason": "Renovate automerges agent-desktop digest bumps, which move every workflow container image with devcontainer-compose-pins.yml (.github/renovate.json); the image name and tag stay unmasked."
        }
      ]
    }
  }
}
